pub struct Operations {}

impl Operations {
    pub fn new(name: &str) {
        todo!("{}", name);
    }

    pub fn preview(port: u16) {
        todo!("{}", &port);
    }

    pub fn publish(rebuild: bool) {
        todo!("{}", &rebuild);
    }
}
